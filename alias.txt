alias cwd="pwd|xclip -i"
alias xc="r|xclip -i"
function tl() {
  if [[ -d $1 ]] ; then
    command r|less
  else
    command cat $1 | less 
  fi 
}
function listeningport() {
  if [[ -d $1 ]] ; then
    command netstat -tulpn
  else
    command netstat -tulpn | grep "$1"
  fi
}